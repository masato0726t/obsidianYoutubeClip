{
  "name": "obsidian-youtube-clip",
  "version": "1.0.0",
  "description": "YouTubeの動画を文字起こしして自動的にObsidianへ保存するChrome拡張機能",
  "private": true,
  "scripts": {
    "icons": "node scripts/generate-icons.js",
    "pack": "node scripts/pack.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "archiver": "^8.0.0",
    "jest": "^29.7.0"
  }
}
